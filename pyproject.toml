[tool.poetry]
name = "vn-stock-ai"
version = "1.0.0"
description = "VN Stock AI"
authors = []
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
